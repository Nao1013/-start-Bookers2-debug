$('.comment').html("<%= j(render "post_comments/post", book: @comment.book) %>");
$('.count').html("<%= j(render "post_comments/count", book: @comment.book) %>");
$("textarea").val("");
$('#exampleModal').modal('hide');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
